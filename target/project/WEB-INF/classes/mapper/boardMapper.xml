<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="Board">
    <insert id="save" parameterType="board" useGeneratedKeys="true" keyProperty="b_id">
        insert into board_t(boardCreatedDate)
        values (now())
    </insert>
    <update id="update" parameterType="board">
        update board_t set boardName = #{boardName}, boardExp = #{boardExp}, boardSecret = #{boardSecret}, boardPassword = #{boardPassword} where b_id = #{b_id}
    </update>
    <select id="findById" parameterType="Long" resultType="board">
        select * from board_t where b_id = #{b_id}
    </select>
</mapper>



